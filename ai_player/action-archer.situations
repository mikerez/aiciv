# AI Civ training situations v1
# Machine row format: correct_output_slot|candidate_output_slots|nonzero_input_slots|target_output_slots|short_description
# Comment blocks explain the game meaning of each slot used by the following machine row.
# situation 0: action archer case: archer sees enemy in range
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: enemy unit signal is visible in local range.
# Decision meaning: attack because archer should shoot visible enemies.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[7] = attack.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[7] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.700000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[58] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
7|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,8:0.700000,9:0.250000,15:0.500000,58:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:-0.900000,7:0.900000|action archer case: archer sees enemy in range
# situation 1: action archer case: archer sees hill firing point
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: north-east local tile is hills.
# Decision meaning: goto because archer should occupy hills for range defense.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[48] = 0.500000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,48:0.500000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000,7:-0.900000|action archer case: archer sees hill firing point
# situation 2: action archer case: archer on hill sees enemy approach
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: enemy approaches while archer already holds hills.
# Decision meaning: wait as fortify/hold because current hill is the right firing position.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.500000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.500000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[58] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,8:0.500000,9:0.500000,15:0.500000,58:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000,7:-0.900000|action archer case: archer on hill sees enemy approach
# situation 3: action archer case: archer holds hill against distant enemy
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: distant enemy is visible while archer is already on hills.
# Decision meaning: wait as hold because defensive hill should be kept when enemy is not adjacent.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.500000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.500000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[59] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,8:0.500000,9:0.500000,15:0.500000,59:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000,7:-0.900000|action archer case: archer holds hill against distant enemy
# situation 4: action archer case: archer holds mountain pass
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: center tile is rocks/mountains defensive terrain.
# Decision meaning: wait as hold because archer is on a strong defensive pass.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.630000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.500000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[56] = 0.630000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,8:0.500000,9:0.630000,15:0.500000,56:0.630000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000,7:-0.900000|action archer case: archer holds mountain pass
# situation 5: action archer case: archer stays on hill with no immediate target
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: center tile is hills and immediate-action signal says defensive hold.
# Decision meaning: wait as hold because no adjacent attack signal is encoded.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.500000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.500000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[56] = 0.500000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,8:0.500000,9:0.500000,15:0.500000,56:0.500000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000,7:-0.900000|action archer case: archer stays on hill with no immediate target
# situation 6: action archer case: archer on hill screens city approach
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: enemy pressure is visible but not adjacent to hill archer.
# Decision meaning: wait as hold because hill position screens the approach better than moving.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.500000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.500000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[55] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,8:0.500000,9:0.500000,15:0.500000,55:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000,7:-0.900000|action archer case: archer on hill screens city approach
# situation 7: action archer case: archer sees enemy city ahead
# Purpose: teach Action engine archer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: enemy city/unit signal ahead.
# Decision meaning: goto because archer should approach siege position before attacking.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.187500 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[57] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1,7|0:0.187500,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,57:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000,7:-0.900000|action archer case: archer sees enemy city ahead
