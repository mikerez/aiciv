# AI Civ training situations v1
# Machine row format: correct_output_slot|candidate_output_slots|nonzero_input_slots|target_output_slots|short_description
# Comment blocks explain the game meaning of each slot used by the following machine row.
# situation 0: action explorer case: explorer sees dark area north
# Purpose: teach Action engine explorer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: north local tile is fog-of-war / unknown.
# Decision meaning: goto because explorers should reveal dark map area.
# Legal output candidates: output[0] = goto; output[1] = wait.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.093750 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[47] = -0.200000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1|0:0.093750,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,47:-0.200000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000|action explorer case: explorer sees dark area north
# situation 1: action explorer case: explorer sees dark area east
# Purpose: teach Action engine explorer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: east local tile is fog-of-war / unknown.
# Decision meaning: goto because moving into fog opens map and resources.
# Legal output candidates: output[0] = goto; output[1] = wait.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.093750 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[57] = -0.200000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1|0:0.093750,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,57:-0.200000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000|action explorer case: explorer sees dark area east
# situation 2: action explorer case: explorer sees city with no nearby resources
# Purpose: teach Action engine explorer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: east local tile is visible city/unit signal but nearby resource score is low.
# Decision meaning: goto city because standing on or near it can reveal resources around it.
# Legal output candidates: output[0] = goto; output[1] = wait.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.093750 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[57] = 0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1|0:0.093750,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,57:0.150000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000|action explorer case: explorer sees city with no nearby resources
# situation 3: action explorer case: explorer on hills sees fog beyond
# Purpose: teach Action engine explorer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: north-east tile is unknown beyond high ground.
# Decision meaning: goto because hills near fog are good scouting points.
# Legal output candidates: output[0] = goto; output[1] = wait.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.093750 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.500000 means current tile terrain type normalized as terrainType/8.
# input[48] = -0.200000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1|0:0.093750,4:1.000000,5:0.200000,6:1.000000,9:0.500000,15:0.500000,48:-0.200000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000|action explorer case: explorer on hills sees fog beyond
# situation 4: action explorer case: explorer surrounded by known land waits
# Purpose: teach Action engine explorer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: local center is ordinary known grass and no fog cue is present.
# Decision meaning: wait because no exploration target is encoded in this compact example.
# Legal output candidates: output[0] = goto; output[1] = wait.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.093750 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[56] = 0.250000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1|0:0.093750,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,56:0.250000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000|action explorer case: explorer surrounded by known land waits
# situation 5: action explorer case: explorer sees coastal unknown
# Purpose: teach Action engine explorer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: farther east local tile is unknown coast or water edge.
# Decision meaning: goto because coast/fog edges are valuable exploration fronts.
# Legal output candidates: output[0] = goto; output[1] = wait.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.093750 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[58] = -0.200000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1|0:0.093750,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,58:-0.200000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000|action explorer case: explorer sees coastal unknown
# situation 6: action explorer case: explorer inside forwarded military focus withdraws
# Purpose: teach Action engine explorer behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: center tile is known grass and strategy focus marks nearby military danger.
# Decision meaning: goto because civilian explorer should leave dangerous focus coordinates while scouting.
# Legal output candidates: output[0] = goto; output[1] = wait.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.093750 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[56] = 0.250000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1|0:0.093750,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,56:0.250000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000|action explorer case: explorer inside forwarded military focus withdraws
