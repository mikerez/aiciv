# AI Civ training situations v1
# Machine row format: correct_output_slot|candidate_output_slots|nonzero_input_slots|target_output_slots|short_description
# Comment blocks explain the game meaning of each slot used by the following machine row.
# situation 0: action warrior case: warrior sees enemy on adjacent grass
# Purpose: teach Action engine warrior behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: east local tile is alien unit on flat land.
# Decision meaning: attack because melee unit can engage adjacent alien on open terrain.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[7] = attack.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[7] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.125000 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.700000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[57] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
7|0,1,7|0:0.125000,4:1.000000,5:0.200000,6:1.000000,8:0.700000,9:0.250000,15:0.500000,57:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:-0.900000,7:0.900000|action warrior case: warrior sees enemy on adjacent grass
# situation 1: action warrior case: warrior sees enemy across open field
# Purpose: teach Action engine warrior behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: farther east local tile is alien unit signal.
# Decision meaning: goto because warrior should close distance before attack.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.125000 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[58] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1,7|0:0.125000,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,58:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000,7:-0.900000|action warrior case: warrior sees enemy across open field
# situation 2: action warrior case: warrior sees hill with no enemy
# Purpose: teach Action engine warrior behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: north-east local tile is hills.
# Decision meaning: goto hill because defensive terrain is valuable before contact.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.125000 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[48] = 0.500000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1,7|0:0.125000,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,48:0.500000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000,7:-0.900000|action warrior case: warrior sees hill with no enemy
# situation 3: action warrior case: warrior already on hill with enemy far away
# Purpose: teach Action engine warrior behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: far enemy is visible while current tile is hills.
# Decision meaning: wait as fortify/hold because hill defense is better than chasing blindly.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.125000 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.500000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.500000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[58] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1,7|0:0.125000,4:1.000000,5:0.200000,6:1.000000,8:0.500000,9:0.500000,15:0.500000,58:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000,7:-0.900000|action warrior case: warrior already on hill with enemy far away
# situation 4: action warrior case: warrior on rocks holds pass
# Purpose: teach Action engine warrior behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: center tile is rocks/mountains defensive terrain.
# Decision meaning: wait as fortify/hold because current terrain is a strong defensive point.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[1] = wait.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.125000 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.630000 means current tile terrain type normalized as terrainType/8.
# input[8] = 0.500000 means immediate action signal: workers use 0.80 improvement, 0.60 irrigation, 0.45 chop, 0.30 road, 0.20 road-to; military uses 0.70 adjacent enemy, 0.50 defensive hill.
# input[56] = 0.630000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
1|0,1,7|0:0.125000,4:1.000000,5:0.200000,6:1.000000,8:0.500000,9:0.630000,15:0.500000,56:0.630000,960:0.500000,965:0.350000,968:0.250000|0:-0.900000,1:0.900000,7:-0.900000|action warrior case: warrior on rocks holds pass
# situation 5: action warrior case: warrior sees enemy city signal
# Purpose: teach Action engine warrior behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: local enemy/city signal is visible at movement distance.
# Decision meaning: goto because warrior should approach before attacking city defenders.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.125000 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[57] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.000000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1,7|0:0.125000,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,57:-0.150000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000,7:-0.900000|action warrior case: warrior sees enemy city signal
# situation 6: action warrior case: warrior follows forwarded military attack focus
# Purpose: teach Action engine warrior behavior from unit status, terrain, resource, fog, city, and local 9x9 window cues.
# Object ids are not encoded. Output command record 0 applies to the first unit id stored by ai.js for object record 0.
# Action object fields used here: input[0]=unit type, input[1]=unit state, input[7]=has active task, input[8]=immediate action signal, input[9]=current terrain, input[10]=current resource value, input[11]=nearby resource score, input[12]=fresh-water flag, input[13]=city plot score or military usefulness, input[14]=age/pressure, input[15]=nearest friendly city distance.
# Local 9x9 window slots are input[16..96], scanned row-major from map offset di=-4,dj=-4 to di=+4,dj=+4. Slot input[56] is the center tile under this unit. Negative local values such as -0.200000 represent fog-of-war or unknown tiles.
# Forwarded strategy focus fields are relative to this unit: input[97]=target dx, input[98]=target dy, input[99]=military attack priority, input[100]=defense or worker-support priority. dx/dy are normalized by the 9x9 window radius of 4 tiles. Browser runtime fills military focus for military records and worker-support focus for worker records; settlers and explorers keep these fields at zero.
# Cue meaning: east local tile has enemy pressure and strategy focus gives attack priority.
# Decision meaning: goto because military units should move toward the strategy attack focus.
# Legal output candidates: output[0] = goto; output[1] = wait; output[7] = attack.
# Correct answer: choose output[0] = goto.
# Evaluation rule: after inference, compare only legal candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other legal candidate output is set to -0.900000.
# input[0] = 0.125000 means unit type normalized as unitTypeIndex/32.
# input[1] = 0.000000 means unit state normalized by the same state order used in ai.js.
# input[7] = 0.000000 means has active task flag; 1.0 means the unit is already busy and should usually preserve its current order.
# input[9] = 0.250000 means current tile terrain type normalized as terrainType/8.
# input[57] = -0.150000 means main local 9x9 cue driving this action.
# input[99] = 0.900000 means forwarded strategy military attack priority.
# Machine row below repeats the same situation in parser format.
0|0,1,7|0:0.125000,4:1.000000,5:0.200000,6:1.000000,9:0.250000,15:0.500000,57:-0.150000,97:0.350000,98:0.100000,99:0.900000,100:0.150000,960:0.500000,965:0.350000,968:0.250000|0:0.900000,1:-0.900000,7:-0.900000|action warrior case: warrior follows forwarded military attack focus
