# AI Civ training situations v1
# Machine row format: correct_output_slot|candidate_output_slots|nonzero_input_slots|target_output_slots|short_description
# Comment blocks explain the game meaning of each slot used by the following machine row.
# situation 0: economics object case 0: object=0 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[0] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other candidate output is set to -0.900000.
# input[0] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[16] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
0|0,1,2,3,4,5,6,7|0:1.000000,8:0.125000,9:0.125000,16:0.600000,960:0.500000|0:0.900000,1:-0.900000,2:-0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 0: object=0 command=0 -> produce Settlers
# situation 1: economics object case 1: object=1 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[8] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[8] is set to +0.900000, every other candidate output is set to -0.900000.
# input[120] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[136] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
8|8,9,10,11,12,13,14,15|120:1.000000,128:0.250000,129:0.125000,136:0.600000,960:0.500000|8:0.900000,9:-0.900000,10:-0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 1: object=1 command=0 -> produce Settlers
# situation 2: economics object case 2: object=2 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[16] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[16] is set to +0.900000, every other candidate output is set to -0.900000.
# input[240] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[256] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
16|16,17,18,19,20,21,22,23|240:1.000000,248:0.375000,249:0.125000,256:0.600000,960:0.500000|16:0.900000,17:-0.900000,18:-0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 2: object=2 command=0 -> produce Settlers
# situation 3: economics object case 3: object=3 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[24] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[24] is set to +0.900000, every other candidate output is set to -0.900000.
# input[360] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[376] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
24|24,25,26,27,28,29,30,31|360:1.000000,368:0.500000,369:0.125000,376:0.600000,960:0.500000|24:0.900000,25:-0.900000,26:-0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 3: object=3 command=0 -> produce Settlers
# situation 4: economics object case 4: object=4 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[32] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[32] is set to +0.900000, every other candidate output is set to -0.900000.
# input[480] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[496] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
32|32,33,34,35,36,37,38,39|480:1.000000,488:0.625000,489:0.125000,496:0.600000,960:0.500000|32:0.900000,33:-0.900000,34:-0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 4: object=4 command=0 -> produce Settlers
# situation 5: economics object case 5: object=5 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[40] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[40] is set to +0.900000, every other candidate output is set to -0.900000.
# input[600] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[616] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
40|40,41,42,43,44,45,46,47|600:1.000000,608:0.750000,609:0.125000,616:0.600000,960:0.500000|40:0.900000,41:-0.900000,42:-0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 5: object=5 command=0 -> produce Settlers
# situation 6: economics object case 6: object=6 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[48] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[48] is set to +0.900000, every other candidate output is set to -0.900000.
# input[720] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[736] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
48|48,49,50,51,52,53,54,55|720:1.000000,728:0.875000,729:0.125000,736:0.600000,960:0.500000|48:0.900000,49:-0.900000,50:-0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 6: object=6 command=0 -> produce Settlers
# situation 7: economics object case 7: object=7 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[56] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[56] is set to +0.900000, every other candidate output is set to -0.900000.
# input[840] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[856] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
56|56,57,58,59,60,61,62,63|840:1.000000,848:1.000000,849:0.125000,856:0.600000,960:0.500000|56:0.900000,57:-0.900000,58:-0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 7: object=7 command=0 -> produce Settlers
# situation 8: economics object case 8: object=0 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[1] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other candidate output is set to -0.900000.
# input[1] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[17] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
1|0,1,2,3,4,5,6,7|1:1.000000,8:0.125000,9:0.250000,17:0.600000,961:0.500000|0:-0.900000,1:0.900000,2:-0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 8: object=0 command=1 -> produce Explorer
# situation 9: economics object case 9: object=1 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[9] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[9] is set to +0.900000, every other candidate output is set to -0.900000.
# input[121] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[137] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
9|8,9,10,11,12,13,14,15|121:1.000000,128:0.250000,129:0.250000,137:0.600000,961:0.500000|8:-0.900000,9:0.900000,10:-0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 9: object=1 command=1 -> produce Explorer
# situation 10: economics object case 10: object=2 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[17] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[17] is set to +0.900000, every other candidate output is set to -0.900000.
# input[241] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[257] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
17|16,17,18,19,20,21,22,23|241:1.000000,248:0.375000,249:0.250000,257:0.600000,961:0.500000|16:-0.900000,17:0.900000,18:-0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 10: object=2 command=1 -> produce Explorer
# situation 11: economics object case 11: object=3 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[25] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[25] is set to +0.900000, every other candidate output is set to -0.900000.
# input[361] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[377] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
25|24,25,26,27,28,29,30,31|361:1.000000,368:0.500000,369:0.250000,377:0.600000,961:0.500000|24:-0.900000,25:0.900000,26:-0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 11: object=3 command=1 -> produce Explorer
# situation 12: economics object case 12: object=4 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[33] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[33] is set to +0.900000, every other candidate output is set to -0.900000.
# input[481] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[497] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
33|32,33,34,35,36,37,38,39|481:1.000000,488:0.625000,489:0.250000,497:0.600000,961:0.500000|32:-0.900000,33:0.900000,34:-0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 12: object=4 command=1 -> produce Explorer
# situation 13: economics object case 13: object=5 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[41] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[41] is set to +0.900000, every other candidate output is set to -0.900000.
# input[601] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[617] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
41|40,41,42,43,44,45,46,47|601:1.000000,608:0.750000,609:0.250000,617:0.600000,961:0.500000|40:-0.900000,41:0.900000,42:-0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 13: object=5 command=1 -> produce Explorer
# situation 14: economics object case 14: object=6 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[49] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[49] is set to +0.900000, every other candidate output is set to -0.900000.
# input[721] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[737] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
49|48,49,50,51,52,53,54,55|721:1.000000,728:0.875000,729:0.250000,737:0.600000,961:0.500000|48:-0.900000,49:0.900000,50:-0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 14: object=6 command=1 -> produce Explorer
# situation 15: economics object case 15: object=7 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[57] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[57] is set to +0.900000, every other candidate output is set to -0.900000.
# input[841] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[857] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
57|56,57,58,59,60,61,62,63|841:1.000000,848:1.000000,849:0.250000,857:0.600000,961:0.500000|56:-0.900000,57:0.900000,58:-0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 15: object=7 command=1 -> produce Explorer
# situation 16: economics object case 16: object=0 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[2] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[2] is set to +0.900000, every other candidate output is set to -0.900000.
# input[2] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[18] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
2|0,1,2,3,4,5,6,7|2:1.000000,8:0.125000,9:0.375000,18:0.600000,962:0.500000|0:-0.900000,1:-0.900000,2:0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 16: object=0 command=2 -> produce Worker
# situation 17: economics object case 17: object=1 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[10] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[10] is set to +0.900000, every other candidate output is set to -0.900000.
# input[122] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[138] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
10|8,9,10,11,12,13,14,15|122:1.000000,128:0.250000,129:0.375000,138:0.600000,962:0.500000|8:-0.900000,9:-0.900000,10:0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 17: object=1 command=2 -> produce Worker
# situation 18: economics object case 18: object=2 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[18] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[18] is set to +0.900000, every other candidate output is set to -0.900000.
# input[242] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[258] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
18|16,17,18,19,20,21,22,23|242:1.000000,248:0.375000,249:0.375000,258:0.600000,962:0.500000|16:-0.900000,17:-0.900000,18:0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 18: object=2 command=2 -> produce Worker
# situation 19: economics object case 19: object=3 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[26] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[26] is set to +0.900000, every other candidate output is set to -0.900000.
# input[362] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[378] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
26|24,25,26,27,28,29,30,31|362:1.000000,368:0.500000,369:0.375000,378:0.600000,962:0.500000|24:-0.900000,25:-0.900000,26:0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 19: object=3 command=2 -> produce Worker
# situation 20: economics object case 20: object=4 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[34] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[34] is set to +0.900000, every other candidate output is set to -0.900000.
# input[482] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[498] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
34|32,33,34,35,36,37,38,39|482:1.000000,488:0.625000,489:0.375000,498:0.600000,962:0.500000|32:-0.900000,33:-0.900000,34:0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 20: object=4 command=2 -> produce Worker
# situation 21: economics object case 21: object=5 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[42] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[42] is set to +0.900000, every other candidate output is set to -0.900000.
# input[602] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[618] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
42|40,41,42,43,44,45,46,47|602:1.000000,608:0.750000,609:0.375000,618:0.600000,962:0.500000|40:-0.900000,41:-0.900000,42:0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 21: object=5 command=2 -> produce Worker
# situation 22: economics object case 22: object=6 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[50] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[50] is set to +0.900000, every other candidate output is set to -0.900000.
# input[722] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[738] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
50|48,49,50,51,52,53,54,55|722:1.000000,728:0.875000,729:0.375000,738:0.600000,962:0.500000|48:-0.900000,49:-0.900000,50:0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 22: object=6 command=2 -> produce Worker
# situation 23: economics object case 23: object=7 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[58] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[58] is set to +0.900000, every other candidate output is set to -0.900000.
# input[842] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[858] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
58|56,57,58,59,60,61,62,63|842:1.000000,848:1.000000,849:0.375000,858:0.600000,962:0.500000|56:-0.900000,57:-0.900000,58:0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 23: object=7 command=2 -> produce Worker
# situation 24: economics object case 24: object=0 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[3] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[3] is set to +0.900000, every other candidate output is set to -0.900000.
# input[3] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[19] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
3|0,1,2,3,4,5,6,7|3:1.000000,8:0.125000,9:0.500000,19:0.600000,963:0.500000|0:-0.900000,1:-0.900000,2:-0.900000,3:0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 24: object=0 command=3 -> produce Warrior
# situation 25: economics object case 25: object=1 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[11] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[11] is set to +0.900000, every other candidate output is set to -0.900000.
# input[123] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[139] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
11|8,9,10,11,12,13,14,15|123:1.000000,128:0.250000,129:0.500000,139:0.600000,963:0.500000|8:-0.900000,9:-0.900000,10:-0.900000,11:0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 25: object=1 command=3 -> produce Warrior
# situation 26: economics object case 26: object=2 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[19] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[19] is set to +0.900000, every other candidate output is set to -0.900000.
# input[243] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[259] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
19|16,17,18,19,20,21,22,23|243:1.000000,248:0.375000,249:0.500000,259:0.600000,963:0.500000|16:-0.900000,17:-0.900000,18:-0.900000,19:0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 26: object=2 command=3 -> produce Warrior
# situation 27: economics object case 27: object=3 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[27] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[27] is set to +0.900000, every other candidate output is set to -0.900000.
# input[363] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[379] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
27|24,25,26,27,28,29,30,31|363:1.000000,368:0.500000,369:0.500000,379:0.600000,963:0.500000|24:-0.900000,25:-0.900000,26:-0.900000,27:0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 27: object=3 command=3 -> produce Warrior
# situation 28: economics object case 28: object=4 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[35] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[35] is set to +0.900000, every other candidate output is set to -0.900000.
# input[483] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[499] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
35|32,33,34,35,36,37,38,39|483:1.000000,488:0.625000,489:0.500000,499:0.600000,963:0.500000|32:-0.900000,33:-0.900000,34:-0.900000,35:0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 28: object=4 command=3 -> produce Warrior
# situation 29: economics object case 29: object=5 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[43] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[43] is set to +0.900000, every other candidate output is set to -0.900000.
# input[603] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[619] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
43|40,41,42,43,44,45,46,47|603:1.000000,608:0.750000,609:0.500000,619:0.600000,963:0.500000|40:-0.900000,41:-0.900000,42:-0.900000,43:0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 29: object=5 command=3 -> produce Warrior
# situation 30: economics object case 30: object=6 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[51] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[51] is set to +0.900000, every other candidate output is set to -0.900000.
# input[723] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[739] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
51|48,49,50,51,52,53,54,55|723:1.000000,728:0.875000,729:0.500000,739:0.600000,963:0.500000|48:-0.900000,49:-0.900000,50:-0.900000,51:0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 30: object=6 command=3 -> produce Warrior
# situation 31: economics object case 31: object=7 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[59] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[59] is set to +0.900000, every other candidate output is set to -0.900000.
# input[843] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[859] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
59|56,57,58,59,60,61,62,63|843:1.000000,848:1.000000,849:0.500000,859:0.600000,963:0.500000|56:-0.900000,57:-0.900000,58:-0.900000,59:0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 31: object=7 command=3 -> produce Warrior
# situation 32: economics object case 32: object=0 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[4] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[4] is set to +0.900000, every other candidate output is set to -0.900000.
# input[4] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[20] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
4|0,1,2,3,4,5,6,7|4:1.000000,8:0.125000,9:0.625000,20:0.600000,964:0.500000|0:-0.900000,1:-0.900000,2:-0.900000,3:-0.900000,4:0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 32: object=0 command=4 -> produce Slinger
# situation 33: economics object case 33: object=1 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[12] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[12] is set to +0.900000, every other candidate output is set to -0.900000.
# input[124] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[140] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
12|8,9,10,11,12,13,14,15|124:1.000000,128:0.250000,129:0.625000,140:0.600000,964:0.500000|8:-0.900000,9:-0.900000,10:-0.900000,11:-0.900000,12:0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 33: object=1 command=4 -> produce Slinger
# situation 34: economics object case 34: object=2 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[20] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[20] is set to +0.900000, every other candidate output is set to -0.900000.
# input[244] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[260] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
20|16,17,18,19,20,21,22,23|244:1.000000,248:0.375000,249:0.625000,260:0.600000,964:0.500000|16:-0.900000,17:-0.900000,18:-0.900000,19:-0.900000,20:0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 34: object=2 command=4 -> produce Slinger
# situation 35: economics object case 35: object=3 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[28] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[28] is set to +0.900000, every other candidate output is set to -0.900000.
# input[364] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[380] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
28|24,25,26,27,28,29,30,31|364:1.000000,368:0.500000,369:0.625000,380:0.600000,964:0.500000|24:-0.900000,25:-0.900000,26:-0.900000,27:-0.900000,28:0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 35: object=3 command=4 -> produce Slinger
# situation 36: economics object case 36: object=4 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[36] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[36] is set to +0.900000, every other candidate output is set to -0.900000.
# input[484] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[500] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
36|32,33,34,35,36,37,38,39|484:1.000000,488:0.625000,489:0.625000,500:0.600000,964:0.500000|32:-0.900000,33:-0.900000,34:-0.900000,35:-0.900000,36:0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 36: object=4 command=4 -> produce Slinger
# situation 37: economics object case 37: object=5 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[44] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[44] is set to +0.900000, every other candidate output is set to -0.900000.
# input[604] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[620] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
44|40,41,42,43,44,45,46,47|604:1.000000,608:0.750000,609:0.625000,620:0.600000,964:0.500000|40:-0.900000,41:-0.900000,42:-0.900000,43:-0.900000,44:0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 37: object=5 command=4 -> produce Slinger
# situation 38: economics object case 38: object=6 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[52] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[52] is set to +0.900000, every other candidate output is set to -0.900000.
# input[724] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[740] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
52|48,49,50,51,52,53,54,55|724:1.000000,728:0.875000,729:0.625000,740:0.600000,964:0.500000|48:-0.900000,49:-0.900000,50:-0.900000,51:-0.900000,52:0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 38: object=6 command=4 -> produce Slinger
# situation 39: economics object case 39: object=7 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[60] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[60] is set to +0.900000, every other candidate output is set to -0.900000.
# input[844] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[860] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
60|56,57,58,59,60,61,62,63|844:1.000000,848:1.000000,849:0.625000,860:0.600000,964:0.500000|56:-0.900000,57:-0.900000,58:-0.900000,59:-0.900000,60:0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 39: object=7 command=4 -> produce Slinger
# situation 40: economics object case 40: object=0 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[5] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[5] is set to +0.900000, every other candidate output is set to -0.900000.
# input[5] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[21] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
5|0,1,2,3,4,5,6,7|5:1.000000,8:0.125000,9:0.750000,21:0.600000,965:0.500000|0:-0.900000,1:-0.900000,2:-0.900000,3:-0.900000,4:-0.900000,5:0.900000,6:-0.900000,7:-0.900000|economics object case 40: object=0 command=5 -> produce Archor
# situation 41: economics object case 41: object=1 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[13] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[13] is set to +0.900000, every other candidate output is set to -0.900000.
# input[125] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[141] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
13|8,9,10,11,12,13,14,15|125:1.000000,128:0.250000,129:0.750000,141:0.600000,965:0.500000|8:-0.900000,9:-0.900000,10:-0.900000,11:-0.900000,12:-0.900000,13:0.900000,14:-0.900000,15:-0.900000|economics object case 41: object=1 command=5 -> produce Archor
# situation 42: economics object case 42: object=2 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[21] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[21] is set to +0.900000, every other candidate output is set to -0.900000.
# input[245] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[261] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
21|16,17,18,19,20,21,22,23|245:1.000000,248:0.375000,249:0.750000,261:0.600000,965:0.500000|16:-0.900000,17:-0.900000,18:-0.900000,19:-0.900000,20:-0.900000,21:0.900000,22:-0.900000,23:-0.900000|economics object case 42: object=2 command=5 -> produce Archor
# situation 43: economics object case 43: object=3 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[29] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[29] is set to +0.900000, every other candidate output is set to -0.900000.
# input[365] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[381] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
29|24,25,26,27,28,29,30,31|365:1.000000,368:0.500000,369:0.750000,381:0.600000,965:0.500000|24:-0.900000,25:-0.900000,26:-0.900000,27:-0.900000,28:-0.900000,29:0.900000,30:-0.900000,31:-0.900000|economics object case 43: object=3 command=5 -> produce Archor
# situation 44: economics object case 44: object=4 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[37] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[37] is set to +0.900000, every other candidate output is set to -0.900000.
# input[485] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[501] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
37|32,33,34,35,36,37,38,39|485:1.000000,488:0.625000,489:0.750000,501:0.600000,965:0.500000|32:-0.900000,33:-0.900000,34:-0.900000,35:-0.900000,36:-0.900000,37:0.900000,38:-0.900000,39:-0.900000|economics object case 44: object=4 command=5 -> produce Archor
# situation 45: economics object case 45: object=5 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[45] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[45] is set to +0.900000, every other candidate output is set to -0.900000.
# input[605] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[621] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
45|40,41,42,43,44,45,46,47|605:1.000000,608:0.750000,609:0.750000,621:0.600000,965:0.500000|40:-0.900000,41:-0.900000,42:-0.900000,43:-0.900000,44:-0.900000,45:0.900000,46:-0.900000,47:-0.900000|economics object case 45: object=5 command=5 -> produce Archor
# situation 46: economics object case 46: object=6 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[53] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[53] is set to +0.900000, every other candidate output is set to -0.900000.
# input[725] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[741] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
53|48,49,50,51,52,53,54,55|725:1.000000,728:0.875000,729:0.750000,741:0.600000,965:0.500000|48:-0.900000,49:-0.900000,50:-0.900000,51:-0.900000,52:-0.900000,53:0.900000,54:-0.900000,55:-0.900000|economics object case 46: object=6 command=5 -> produce Archor
# situation 47: economics object case 47: object=7 command=5 -> produce Archor
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 5; intended reading is "produce Archor".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[61] = produce Archor.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[61] is set to +0.900000, every other candidate output is set to -0.900000.
# input[845] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[861] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[965] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
61|56,57,58,59,60,61,62,63|845:1.000000,848:1.000000,849:0.750000,861:0.600000,965:0.500000|56:-0.900000,57:-0.900000,58:-0.900000,59:-0.900000,60:-0.900000,61:0.900000,62:-0.900000,63:-0.900000|economics object case 47: object=7 command=5 -> produce Archor
# situation 48: economics object case 48: object=0 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[6] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[6] is set to +0.900000, every other candidate output is set to -0.900000.
# input[6] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[22] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
6|0,1,2,3,4,5,6,7|6:1.000000,8:0.125000,9:0.875000,22:0.600000,966:0.500000|0:-0.900000,1:-0.900000,2:-0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:0.900000,7:-0.900000|economics object case 48: object=0 command=6 -> produce Spearman
# situation 49: economics object case 49: object=1 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[14] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[14] is set to +0.900000, every other candidate output is set to -0.900000.
# input[126] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[142] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
14|8,9,10,11,12,13,14,15|126:1.000000,128:0.250000,129:0.875000,142:0.600000,966:0.500000|8:-0.900000,9:-0.900000,10:-0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:0.900000,15:-0.900000|economics object case 49: object=1 command=6 -> produce Spearman
# situation 50: economics object case 50: object=2 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[22] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[22] is set to +0.900000, every other candidate output is set to -0.900000.
# input[246] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[262] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
22|16,17,18,19,20,21,22,23|246:1.000000,248:0.375000,249:0.875000,262:0.600000,966:0.500000|16:-0.900000,17:-0.900000,18:-0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:0.900000,23:-0.900000|economics object case 50: object=2 command=6 -> produce Spearman
# situation 51: economics object case 51: object=3 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[30] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[30] is set to +0.900000, every other candidate output is set to -0.900000.
# input[366] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[382] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
30|24,25,26,27,28,29,30,31|366:1.000000,368:0.500000,369:0.875000,382:0.600000,966:0.500000|24:-0.900000,25:-0.900000,26:-0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:0.900000,31:-0.900000|economics object case 51: object=3 command=6 -> produce Spearman
# situation 52: economics object case 52: object=4 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[38] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[38] is set to +0.900000, every other candidate output is set to -0.900000.
# input[486] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[502] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
38|32,33,34,35,36,37,38,39|486:1.000000,488:0.625000,489:0.875000,502:0.600000,966:0.500000|32:-0.900000,33:-0.900000,34:-0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:0.900000,39:-0.900000|economics object case 52: object=4 command=6 -> produce Spearman
# situation 53: economics object case 53: object=5 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[46] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[46] is set to +0.900000, every other candidate output is set to -0.900000.
# input[606] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[622] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
46|40,41,42,43,44,45,46,47|606:1.000000,608:0.750000,609:0.875000,622:0.600000,966:0.500000|40:-0.900000,41:-0.900000,42:-0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:0.900000,47:-0.900000|economics object case 53: object=5 command=6 -> produce Spearman
# situation 54: economics object case 54: object=6 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[54] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[54] is set to +0.900000, every other candidate output is set to -0.900000.
# input[726] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[742] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
54|48,49,50,51,52,53,54,55|726:1.000000,728:0.875000,729:0.875000,742:0.600000,966:0.500000|48:-0.900000,49:-0.900000,50:-0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:0.900000,55:-0.900000|economics object case 54: object=6 command=6 -> produce Spearman
# situation 55: economics object case 55: object=7 command=6 -> produce Spearman
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 6; intended reading is "produce Spearman".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[62] = produce Spearman.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[62] is set to +0.900000, every other candidate output is set to -0.900000.
# input[846] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[862] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[966] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
62|56,57,58,59,60,61,62,63|846:1.000000,848:1.000000,849:0.875000,862:0.600000,966:0.500000|56:-0.900000,57:-0.900000,58:-0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:0.900000,63:-0.900000|economics object case 55: object=7 command=6 -> produce Spearman
# situation 56: economics object case 56: object=0 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 7; intended reading is "produce None".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[7] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[7] is set to +0.900000, every other candidate output is set to -0.900000.
# input[7] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[23] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
7|0,1,2,3,4,5,6,7|7:1.000000,8:0.125000,9:1.000000,23:0.600000,967:0.500000|0:-0.900000,1:-0.900000,2:-0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:0.900000|economics object case 56: object=0 command=7 -> produce None
# situation 57: economics object case 57: object=1 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 7; intended reading is "produce None".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[15] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[15] is set to +0.900000, every other candidate output is set to -0.900000.
# input[127] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[143] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
15|8,9,10,11,12,13,14,15|127:1.000000,128:0.250000,129:1.000000,143:0.600000,967:0.500000|8:-0.900000,9:-0.900000,10:-0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:0.900000|economics object case 57: object=1 command=7 -> produce None
# situation 58: economics object case 58: object=2 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 7; intended reading is "produce None".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[23] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[23] is set to +0.900000, every other candidate output is set to -0.900000.
# input[247] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[263] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
23|16,17,18,19,20,21,22,23|247:1.000000,248:0.375000,249:1.000000,263:0.600000,967:0.500000|16:-0.900000,17:-0.900000,18:-0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:0.900000|economics object case 58: object=2 command=7 -> produce None
# situation 59: economics object case 59: object=3 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 7; intended reading is "produce None".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[31] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[31] is set to +0.900000, every other candidate output is set to -0.900000.
# input[367] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[383] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
31|24,25,26,27,28,29,30,31|367:1.000000,368:0.500000,369:1.000000,383:0.600000,967:0.500000|24:-0.900000,25:-0.900000,26:-0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:0.900000|economics object case 59: object=3 command=7 -> produce None
# situation 60: economics object case 60: object=4 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 7; intended reading is "produce None".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[39] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[39] is set to +0.900000, every other candidate output is set to -0.900000.
# input[487] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[503] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
39|32,33,34,35,36,37,38,39|487:1.000000,488:0.625000,489:1.000000,503:0.600000,967:0.500000|32:-0.900000,33:-0.900000,34:-0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:0.900000|economics object case 60: object=4 command=7 -> produce None
# situation 61: economics object case 61: object=5 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 7; intended reading is "produce None".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[47] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[47] is set to +0.900000, every other candidate output is set to -0.900000.
# input[607] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[623] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
47|40,41,42,43,44,45,46,47|607:1.000000,608:0.750000,609:1.000000,623:0.600000,967:0.500000|40:-0.900000,41:-0.900000,42:-0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:0.900000|economics object case 61: object=5 command=7 -> produce None
# situation 62: economics object case 62: object=6 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 7; intended reading is "produce None".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[55] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[55] is set to +0.900000, every other candidate output is set to -0.900000.
# input[727] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[743] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
55|48,49,50,51,52,53,54,55|727:1.000000,728:0.875000,729:1.000000,743:0.600000,967:0.500000|48:-0.900000,49:-0.900000,50:-0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:0.900000|economics object case 62: object=6 command=7 -> produce None
# situation 63: economics object case 63: object=7 command=7 -> produce None
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 7; intended reading is "produce None".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[63] = produce None.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[63] is set to +0.900000, every other candidate output is set to -0.900000.
# input[847] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[863] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[967] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
63|56,57,58,59,60,61,62,63|847:1.000000,848:1.000000,849:1.000000,863:0.600000,967:0.500000|56:-0.900000,57:-0.900000,58:-0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:0.900000|economics object case 63: object=7 command=7 -> produce None
# situation 64: economics object case 64: object=0 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[0] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[0] is set to +0.900000, every other candidate output is set to -0.900000.
# input[0] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[16] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
0|0,1,2,3,4,5,6,7|0:1.000000,8:0.125000,9:0.125000,16:0.600000,960:0.500000|0:0.900000,1:-0.900000,2:-0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 64: object=0 command=0 -> produce Settlers
# situation 65: economics object case 65: object=1 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[8] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[8] is set to +0.900000, every other candidate output is set to -0.900000.
# input[120] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[136] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
8|8,9,10,11,12,13,14,15|120:1.000000,128:0.250000,129:0.125000,136:0.600000,960:0.500000|8:0.900000,9:-0.900000,10:-0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 65: object=1 command=0 -> produce Settlers
# situation 66: economics object case 66: object=2 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[16] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[16] is set to +0.900000, every other candidate output is set to -0.900000.
# input[240] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[256] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
16|16,17,18,19,20,21,22,23|240:1.000000,248:0.375000,249:0.125000,256:0.600000,960:0.500000|16:0.900000,17:-0.900000,18:-0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 66: object=2 command=0 -> produce Settlers
# situation 67: economics object case 67: object=3 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[24] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[24] is set to +0.900000, every other candidate output is set to -0.900000.
# input[360] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[376] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
24|24,25,26,27,28,29,30,31|360:1.000000,368:0.500000,369:0.125000,376:0.600000,960:0.500000|24:0.900000,25:-0.900000,26:-0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 67: object=3 command=0 -> produce Settlers
# situation 68: economics object case 68: object=4 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[32] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[32] is set to +0.900000, every other candidate output is set to -0.900000.
# input[480] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[496] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
32|32,33,34,35,36,37,38,39|480:1.000000,488:0.625000,489:0.125000,496:0.600000,960:0.500000|32:0.900000,33:-0.900000,34:-0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 68: object=4 command=0 -> produce Settlers
# situation 69: economics object case 69: object=5 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[40] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[40] is set to +0.900000, every other candidate output is set to -0.900000.
# input[600] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[616] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
40|40,41,42,43,44,45,46,47|600:1.000000,608:0.750000,609:0.125000,616:0.600000,960:0.500000|40:0.900000,41:-0.900000,42:-0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 69: object=5 command=0 -> produce Settlers
# situation 70: economics object case 70: object=6 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[48] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[48] is set to +0.900000, every other candidate output is set to -0.900000.
# input[720] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[736] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
48|48,49,50,51,52,53,54,55|720:1.000000,728:0.875000,729:0.125000,736:0.600000,960:0.500000|48:0.900000,49:-0.900000,50:-0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 70: object=6 command=0 -> produce Settlers
# situation 71: economics object case 71: object=7 command=0 -> produce Settlers
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 0; intended reading is "produce Settlers".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[56] = produce Settlers.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[56] is set to +0.900000, every other candidate output is set to -0.900000.
# input[840] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[856] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[960] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
56|56,57,58,59,60,61,62,63|840:1.000000,848:1.000000,849:0.125000,856:0.600000,960:0.500000|56:0.900000,57:-0.900000,58:-0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 71: object=7 command=0 -> produce Settlers
# situation 72: economics object case 72: object=0 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[1] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[1] is set to +0.900000, every other candidate output is set to -0.900000.
# input[1] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[17] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
1|0,1,2,3,4,5,6,7|1:1.000000,8:0.125000,9:0.250000,17:0.600000,961:0.500000|0:-0.900000,1:0.900000,2:-0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 72: object=0 command=1 -> produce Explorer
# situation 73: economics object case 73: object=1 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[9] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[9] is set to +0.900000, every other candidate output is set to -0.900000.
# input[121] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[137] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
9|8,9,10,11,12,13,14,15|121:1.000000,128:0.250000,129:0.250000,137:0.600000,961:0.500000|8:-0.900000,9:0.900000,10:-0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 73: object=1 command=1 -> produce Explorer
# situation 74: economics object case 74: object=2 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[17] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[17] is set to +0.900000, every other candidate output is set to -0.900000.
# input[241] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[257] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
17|16,17,18,19,20,21,22,23|241:1.000000,248:0.375000,249:0.250000,257:0.600000,961:0.500000|16:-0.900000,17:0.900000,18:-0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 74: object=2 command=1 -> produce Explorer
# situation 75: economics object case 75: object=3 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[25] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[25] is set to +0.900000, every other candidate output is set to -0.900000.
# input[361] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[377] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
25|24,25,26,27,28,29,30,31|361:1.000000,368:0.500000,369:0.250000,377:0.600000,961:0.500000|24:-0.900000,25:0.900000,26:-0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 75: object=3 command=1 -> produce Explorer
# situation 76: economics object case 76: object=4 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[33] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[33] is set to +0.900000, every other candidate output is set to -0.900000.
# input[481] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[497] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
33|32,33,34,35,36,37,38,39|481:1.000000,488:0.625000,489:0.250000,497:0.600000,961:0.500000|32:-0.900000,33:0.900000,34:-0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 76: object=4 command=1 -> produce Explorer
# situation 77: economics object case 77: object=5 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[41] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[41] is set to +0.900000, every other candidate output is set to -0.900000.
# input[601] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[617] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
41|40,41,42,43,44,45,46,47|601:1.000000,608:0.750000,609:0.250000,617:0.600000,961:0.500000|40:-0.900000,41:0.900000,42:-0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 77: object=5 command=1 -> produce Explorer
# situation 78: economics object case 78: object=6 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[49] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[49] is set to +0.900000, every other candidate output is set to -0.900000.
# input[721] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[737] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
49|48,49,50,51,52,53,54,55|721:1.000000,728:0.875000,729:0.250000,737:0.600000,961:0.500000|48:-0.900000,49:0.900000,50:-0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 78: object=6 command=1 -> produce Explorer
# situation 79: economics object case 79: object=7 command=1 -> produce Explorer
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 1; intended reading is "produce Explorer".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[57] = produce Explorer.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[57] is set to +0.900000, every other candidate output is set to -0.900000.
# input[841] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[857] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[961] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
57|56,57,58,59,60,61,62,63|841:1.000000,848:1.000000,849:0.250000,857:0.600000,961:0.500000|56:-0.900000,57:0.900000,58:-0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 79: object=7 command=1 -> produce Explorer
# situation 80: economics object case 80: object=0 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[2] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[2] is set to +0.900000, every other candidate output is set to -0.900000.
# input[2] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[18] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
2|0,1,2,3,4,5,6,7|2:1.000000,8:0.125000,9:0.375000,18:0.600000,962:0.500000|0:-0.900000,1:-0.900000,2:0.900000,3:-0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 80: object=0 command=2 -> produce Worker
# situation 81: economics object case 81: object=1 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[10] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[10] is set to +0.900000, every other candidate output is set to -0.900000.
# input[122] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[138] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
10|8,9,10,11,12,13,14,15|122:1.000000,128:0.250000,129:0.375000,138:0.600000,962:0.500000|8:-0.900000,9:-0.900000,10:0.900000,11:-0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 81: object=1 command=2 -> produce Worker
# situation 82: economics object case 82: object=2 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[18] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[18] is set to +0.900000, every other candidate output is set to -0.900000.
# input[242] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[258] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
18|16,17,18,19,20,21,22,23|242:1.000000,248:0.375000,249:0.375000,258:0.600000,962:0.500000|16:-0.900000,17:-0.900000,18:0.900000,19:-0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 82: object=2 command=2 -> produce Worker
# situation 83: economics object case 83: object=3 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[26] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[26] is set to +0.900000, every other candidate output is set to -0.900000.
# input[362] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[378] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
26|24,25,26,27,28,29,30,31|362:1.000000,368:0.500000,369:0.375000,378:0.600000,962:0.500000|24:-0.900000,25:-0.900000,26:0.900000,27:-0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 83: object=3 command=2 -> produce Worker
# situation 84: economics object case 84: object=4 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[34] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[34] is set to +0.900000, every other candidate output is set to -0.900000.
# input[482] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[498] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
34|32,33,34,35,36,37,38,39|482:1.000000,488:0.625000,489:0.375000,498:0.600000,962:0.500000|32:-0.900000,33:-0.900000,34:0.900000,35:-0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 84: object=4 command=2 -> produce Worker
# situation 85: economics object case 85: object=5 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[42] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[42] is set to +0.900000, every other candidate output is set to -0.900000.
# input[602] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[618] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
42|40,41,42,43,44,45,46,47|602:1.000000,608:0.750000,609:0.375000,618:0.600000,962:0.500000|40:-0.900000,41:-0.900000,42:0.900000,43:-0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 85: object=5 command=2 -> produce Worker
# situation 86: economics object case 86: object=6 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[50] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[50] is set to +0.900000, every other candidate output is set to -0.900000.
# input[722] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[738] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
50|48,49,50,51,52,53,54,55|722:1.000000,728:0.875000,729:0.375000,738:0.600000,962:0.500000|48:-0.900000,49:-0.900000,50:0.900000,51:-0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 86: object=6 command=2 -> produce Worker
# situation 87: economics object case 87: object=7 command=2 -> produce Worker
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 2; intended reading is "produce Worker".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[58] = produce Worker.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[58] is set to +0.900000, every other candidate output is set to -0.900000.
# input[842] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[858] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[962] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
58|56,57,58,59,60,61,62,63|842:1.000000,848:1.000000,849:0.375000,858:0.600000,962:0.500000|56:-0.900000,57:-0.900000,58:0.900000,59:-0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 87: object=7 command=2 -> produce Worker
# situation 88: economics object case 88: object=0 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[3] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[3] is set to +0.900000, every other candidate output is set to -0.900000.
# input[3] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[19] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
3|0,1,2,3,4,5,6,7|3:1.000000,8:0.125000,9:0.500000,19:0.600000,963:0.500000|0:-0.900000,1:-0.900000,2:-0.900000,3:0.900000,4:-0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 88: object=0 command=3 -> produce Warrior
# situation 89: economics object case 89: object=1 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[11] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[11] is set to +0.900000, every other candidate output is set to -0.900000.
# input[123] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[139] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
11|8,9,10,11,12,13,14,15|123:1.000000,128:0.250000,129:0.500000,139:0.600000,963:0.500000|8:-0.900000,9:-0.900000,10:-0.900000,11:0.900000,12:-0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 89: object=1 command=3 -> produce Warrior
# situation 90: economics object case 90: object=2 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[19] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[19] is set to +0.900000, every other candidate output is set to -0.900000.
# input[243] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[259] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
19|16,17,18,19,20,21,22,23|243:1.000000,248:0.375000,249:0.500000,259:0.600000,963:0.500000|16:-0.900000,17:-0.900000,18:-0.900000,19:0.900000,20:-0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 90: object=2 command=3 -> produce Warrior
# situation 91: economics object case 91: object=3 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[27] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[27] is set to +0.900000, every other candidate output is set to -0.900000.
# input[363] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[379] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
27|24,25,26,27,28,29,30,31|363:1.000000,368:0.500000,369:0.500000,379:0.600000,963:0.500000|24:-0.900000,25:-0.900000,26:-0.900000,27:0.900000,28:-0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 91: object=3 command=3 -> produce Warrior
# situation 92: economics object case 92: object=4 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 4 back to input object 4.
# Situation facts: object record 4 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[32] = produce Settlers; output[33] = produce Explorer; output[34] = produce Worker; output[35] = produce Warrior; output[36] = produce Slinger; output[37] = produce Archor; output[38] = produce Spearman; output[39] = produce None.
# Correct answer: choose output[35] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[35] is set to +0.900000, every other candidate output is set to -0.900000.
# input[483] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[488] = 0.625000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[499] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
35|32,33,34,35,36,37,38,39|483:1.000000,488:0.625000,489:0.500000,499:0.600000,963:0.500000|32:-0.900000,33:-0.900000,34:-0.900000,35:0.900000,36:-0.900000,37:-0.900000,38:-0.900000,39:-0.900000|economics object case 92: object=4 command=3 -> produce Warrior
# situation 93: economics object case 93: object=5 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 5 back to input object 5.
# Situation facts: object record 5 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[40] = produce Settlers; output[41] = produce Explorer; output[42] = produce Worker; output[43] = produce Warrior; output[44] = produce Slinger; output[45] = produce Archor; output[46] = produce Spearman; output[47] = produce None.
# Correct answer: choose output[43] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[43] is set to +0.900000, every other candidate output is set to -0.900000.
# input[603] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[608] = 0.750000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[619] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
43|40,41,42,43,44,45,46,47|603:1.000000,608:0.750000,609:0.500000,619:0.600000,963:0.500000|40:-0.900000,41:-0.900000,42:-0.900000,43:0.900000,44:-0.900000,45:-0.900000,46:-0.900000,47:-0.900000|economics object case 93: object=5 command=3 -> produce Warrior
# situation 94: economics object case 94: object=6 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 6 back to input object 6.
# Situation facts: object record 6 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[48] = produce Settlers; output[49] = produce Explorer; output[50] = produce Worker; output[51] = produce Warrior; output[52] = produce Slinger; output[53] = produce Archor; output[54] = produce Spearman; output[55] = produce None.
# Correct answer: choose output[51] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[51] is set to +0.900000, every other candidate output is set to -0.900000.
# input[723] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[728] = 0.875000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[739] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
51|48,49,50,51,52,53,54,55|723:1.000000,728:0.875000,729:0.500000,739:0.600000,963:0.500000|48:-0.900000,49:-0.900000,50:-0.900000,51:0.900000,52:-0.900000,53:-0.900000,54:-0.900000,55:-0.900000|economics object case 94: object=6 command=3 -> produce Warrior
# situation 95: economics object case 95: object=7 command=3 -> produce Warrior
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 7 back to input object 7.
# Situation facts: object record 7 has command cue 3; intended reading is "produce Warrior".
# Output candidates: output[56] = produce Settlers; output[57] = produce Explorer; output[58] = produce Worker; output[59] = produce Warrior; output[60] = produce Slinger; output[61] = produce Archor; output[62] = produce Spearman; output[63] = produce None.
# Correct answer: choose output[59] = produce Warrior.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[59] is set to +0.900000, every other candidate output is set to -0.900000.
# input[843] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[848] = 1.000000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[859] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[963] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
59|56,57,58,59,60,61,62,63|843:1.000000,848:1.000000,849:0.500000,859:0.600000,963:0.500000|56:-0.900000,57:-0.900000,58:-0.900000,59:0.900000,60:-0.900000,61:-0.900000,62:-0.900000,63:-0.900000|economics object case 95: object=7 command=3 -> produce Warrior
# situation 96: economics object case 96: object=0 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 0 back to input object 0.
# Situation facts: object record 0 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[0] = produce Settlers; output[1] = produce Explorer; output[2] = produce Worker; output[3] = produce Warrior; output[4] = produce Slinger; output[5] = produce Archor; output[6] = produce Spearman; output[7] = produce None.
# Correct answer: choose output[4] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[4] is set to +0.900000, every other candidate output is set to -0.900000.
# input[4] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[8] = 0.125000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[20] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
4|0,1,2,3,4,5,6,7|4:1.000000,8:0.125000,9:0.625000,20:0.600000,964:0.500000|0:-0.900000,1:-0.900000,2:-0.900000,3:-0.900000,4:0.900000,5:-0.900000,6:-0.900000,7:-0.900000|economics object case 96: object=0 command=4 -> produce Slinger
# situation 97: economics object case 97: object=1 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 1 back to input object 1.
# Situation facts: object record 1 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[8] = produce Settlers; output[9] = produce Explorer; output[10] = produce Worker; output[11] = produce Warrior; output[12] = produce Slinger; output[13] = produce Archor; output[14] = produce Spearman; output[15] = produce None.
# Correct answer: choose output[12] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[12] is set to +0.900000, every other candidate output is set to -0.900000.
# input[124] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[128] = 0.250000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[140] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
12|8,9,10,11,12,13,14,15|124:1.000000,128:0.250000,129:0.625000,140:0.600000,964:0.500000|8:-0.900000,9:-0.900000,10:-0.900000,11:-0.900000,12:0.900000,13:-0.900000,14:-0.900000,15:-0.900000|economics object case 97: object=1 command=4 -> produce Slinger
# situation 98: economics object case 98: object=2 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 2 back to input object 2.
# Situation facts: object record 2 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[16] = produce Settlers; output[17] = produce Explorer; output[18] = produce Worker; output[19] = produce Warrior; output[20] = produce Slinger; output[21] = produce Archor; output[22] = produce Spearman; output[23] = produce None.
# Correct answer: choose output[20] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[20] is set to +0.900000, every other candidate output is set to -0.900000.
# input[244] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[248] = 0.375000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[260] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
20|16,17,18,19,20,21,22,23|244:1.000000,248:0.375000,249:0.625000,260:0.600000,964:0.500000|16:-0.900000,17:-0.900000,18:-0.900000,19:-0.900000,20:0.900000,21:-0.900000,22:-0.900000,23:-0.900000|economics object case 98: object=2 command=4 -> produce Slinger
# situation 99: economics object case 99: object=3 command=4 -> produce Slinger
# Purpose: train the economics engine to choose one 8-float command for one object record.
# Object ids are not encoded. The game keeps ids in a side array and maps output command record 3 back to input object 3.
# Situation facts: object record 3 has command cue 4; intended reading is "produce Slinger".
# Output candidates: output[24] = produce Settlers; output[25] = produce Explorer; output[26] = produce Worker; output[27] = produce Warrior; output[28] = produce Slinger; output[29] = produce Archor; output[30] = produce Spearman; output[31] = produce None.
# Correct answer: choose output[28] = produce Slinger.
# Evaluation rule: after inference, compare only those candidate outputs; the highest value is the predicted answer.
# Training target: output[28] is set to +0.900000, every other candidate output is set to -0.900000.
# input[364] = 1.000000 means object-local command cue field inside the 120-float object record.
# input[368] = 0.500000 means object-order marker used only by bootstrap examples, not a persistent object id.
# input[380] = 0.600000 means local 9x9 window or object-detail feature supporting the command.
# input[964] = 0.500000 means generic situation field supporting the same command class.
# Machine row below repeats the same situation in parser format.
28|24,25,26,27,28,29,30,31|364:1.000000,368:0.500000,369:0.625000,380:0.600000,964:0.500000|24:-0.900000,25:-0.900000,26:-0.900000,27:-0.900000,28:0.900000,29:-0.900000,30:-0.900000,31:-0.900000|economics object case 99: object=3 command=4 -> produce Slinger
