; Install as /etc/php-fpm.d/zz-aiciv.conf on the small EC2 game host.
; The default dynamic pool can retain hundreds of megabytes in idle workers.
[www]
pm = ondemand
pm.max_children = 6
pm.process_idle_timeout = 10s
pm.max_requests = 200
